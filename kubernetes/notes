kubernetes is for orchestration/manage a large numbersof  conatiners
It Works on master-worker architecture
it has one master node(Brain) & worker nodes(muscle), master node one be one in numbers, but worker node could be more than one, it depends on you that how much you want worker nodes
